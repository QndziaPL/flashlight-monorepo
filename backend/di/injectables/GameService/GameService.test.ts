import { GameService } from "./GameService";

describe("GameService", () => {
  it("asd", () => {
    expect(1).toEqual(1);
  });
  // let gameService: GameService;
  // let mockLobbyService: jest.Mocked<LobbyService>;
  // let mockWebsocketClient: jest.Mocked<WebSocketClient>;
  //
  // beforeEach(() => {
  //   mockLobbyService = new LobbyService() as jest.Mocked<LobbyService>;
  //   mockWebsocketClient = new WebSocketClient() as jest.Mocked<WebSocketClient>;
  // });
});
